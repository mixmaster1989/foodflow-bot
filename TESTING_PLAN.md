# 🧪 План тестирования FoodFlow Bot

## 📋 Инструкция для тестирования

Этот план поможет протестировать все функции бота вживую. После каждого шага я буду анализировать логи и подтверждать, что всё работает корректно.

**Важно:**
- Бот запущен через PM2: `pm2 restart foodflow-bot`
- Логи пишутся в `foodflow.log` и в консоль
- После каждого шага делай паузу, чтобы я мог проверить логи
- Если что-то не работает - сразу сообщи, я проверю логи

## ✅ Статус тестирования

**Протестировано:**
- ✅ ЭТАП 1: Базовая инициализация (`/start`)
- ✅ ЭТАП 1.1: Onboarding (первичная настройка профиля)
- ✅ ЭТАП 3: Загрузка чека (OCR, нормализация, сохранение)
  - ✅ Исправлен порядок сообщений (сначала товары, потом итог с кнопкой "Назад")
  - ✅ Добавлена кнопка "Назад к меню" после обработки чека
  - ✅ Улучшены сообщения об ошибках (убраны технические детали)
- ✅ ЭТАП 4: Управление холодильником (Fridge)
  - ✅ Просмотр списка продуктов с пагинацией
  - ✅ Детальный просмотр продукта
  - ✅ Удаление продукта
  - ✅ Потребление продукта (Съел)
- ✅ ЭТАП 2: Главное меню и навигация
  - ✅ Просмотр главного меню
  - ✅ Навигация по меню (Справка, Настройки, возврат)
  - ✅ Все кнопки главного меню работают корректно
- ✅ ЭТАП 10: Коррекция продуктов (полностью переработана)
  - ✅ Новая логика: карточка продукта с кнопками выбора поля
  - ✅ Редактирование: название, цена, количество, категория
  - ✅ Выбор категории из списка кнопок

**В процессе:**
- 🔄 ЭТАП 5: Генерация рецептов

**Ожидает тестирования:**
- ⏳ ЭТАП 1.2: Консультант (AI рекомендации по продуктам)
- ⏳ ЭТАП 6: Статистика
- ⏳ ЭТАП 7: Список покупок
- ⏳ ЭТАП 8: Режим покупок
- ⏳ ЭТАП 9: Настройки пользователя
- ⏳ ЭТАП 11: Обработка ошибок

---

## ✅ ЭТАП 1: Базовая инициализация

### Шаг 1.1: Первый запуск бота (новый пользователь)
**Действие:**
1. Отправь боту команду `/start` (для нового пользователя или если `is_initialized=False`)

**Что проверяю в логах:**
- ✅ Создание пользователя в БД (`User created`)
- ✅ Проверка `UserSettings.is_initialized`
- ✅ Если `False` - запуск onboarding (показ выбора пола)
- ✅ Если `True` - показ главного меню

**Ожидаемый результат:**
- Для нового пользователя: показывается onboarding (выбор пола)
- Для существующего: показывается главное меню

### Шаг 1.2: Onboarding - выбор пола
**Действие:**
1. Нажми "👨 Мужской" или "👩 Женский"

**Что проверяю:**
- ✅ Callback `onboarding_gender:*` обрабатывается
- ✅ FSM состояние `waiting_for_height`
- ✅ Показывается запрос на ввод роста

### Шаг 1.3: Onboarding - ввод роста
**Действие:**
1. Введи рост в см (например: 175)

**Что проверяю:**
- ✅ Валидация ввода
- ✅ FSM состояние `waiting_for_weight`
- ✅ Показывается запрос на ввод веса

### Шаг 1.4: Onboarding - ввод веса
**Действие:**
1. Введи вес в кг (например: 70.5)

**Что проверяю:**
- ✅ Валидация ввода
- ✅ FSM состояние `waiting_for_goal`
- ✅ Показывается меню выбора цели

### Шаг 1.5: Onboarding - выбор цели
**Действие:**
1. Выбери цель (Похудеть/Не толстеть/Здоровое питание/Набрать массу)

**Что проверяю:**
- ✅ Сохранение всех данных в `UserSettings`
- ✅ `is_initialized=True`
- ✅ Показывается главное меню

**Ожидаемый результат:**
- Профиль сохранен, показывается главное меню

---

## ✅ ЭТАП 2: Главное меню и навигация

### Шаг 2.1: Просмотр главного меню
**Действие:**
1. Нажми кнопку "🔙 Назад" (если есть) или отправь `/start` снова

**Что проверяю:**
- ✅ Корректная загрузка изображения `main_menu.png`
- ✅ Все кнопки отображаются
- ✅ Callback `main_menu` обрабатывается

### Шаг 2.2: Навигация по меню
**Действие:**
1. Нажми "ℹ️ Справка"
2. Нажми "🔙 Назад"
3. Нажми "⚙️ Настройки"
4. Нажми "🔙 Назад"

**Что проверяю:**
- ✅ Callback `menu_help` → показ help.png
- ✅ Callback `menu_settings` → показ settings.png
- ✅ Возврат в главное меню работает
- ✅ Нет ошибок при переключении между экранами

---

## ✅ ЭТАП 3: Загрузка чека (Receipt Processing)

### Шаг 3.1: Инструкция по загрузке чека
**Действие:**
1. Нажми "📸 Загрузить чек"
2. Прочитай инструкцию

**Что проверяю:**
- ✅ Callback `menu_check` обрабатывается
- ✅ Показывается `check_upload.png`
- ✅ Инструкция отображается корректно

### Шаг 3.2: Загрузка фото чека
**Действие:**
1. Вернись в главное меню
2. Отправь боту фото чека (любое фото с текстом/чеком)

**Что проверяю:**
- ✅ `handle_photo` срабатывает
- ✅ Показывается меню выбора действия (чек/ценник/еда)
- ✅ Нет ошибок при обработке фото

### Шаг 3.3: Обработка чека через OCR
**Действие:**
1. Нажми "📸 Это чек"
2. Дождись обработки (может занять 15-45 секунд)

**Что проверяю:**
- ✅ Callback `receipt_action` обрабатывается
- ✅ OCR сервис вызывается (`OCRService.extract_items`)
- ✅ Логирование процесса OCR (`Processing receipt with OCR`)
- ✅ Нормализация продуктов (`NormalizationService.normalize_products`)
- ✅ Сохранение в БД (`Receipt created`, `Products saved`)
- ✅ Показ итогового сообщения с продуктами
- ✅ Время обработки < 60 секунд
- ✅ Нет ошибок API (OpenRouter)

**Ожидаемый результат:**
- Бот показывает список распознанных продуктов с ценами (каждый продукт отдельным сообщением с кнопкой "✏️ Коррекция")
- Последним сообщением показывается итоговая сводка с кнопкой "🔙 Назад к меню"
- Продукты сохранены в холодильник
- Если товары не распознаны - показывается соответствующее сообщение

### Шаг 3.4: Проверка сохранения в БД
**Действие:**
1. Перейди в "🧊 Холодильник"
2. Проверь, что продукты из чека там есть

**Что проверяю:**
- ✅ Продукты отображаются в `list_fridge`
- ✅ Корректный SQL запрос к БД
- ✅ Нет ошибок при выборке продуктов

---

## ✅ ЭТАП 4: Управление холодильником (Fridge)

### Шаг 4.1: Просмотр холодильника (пустой)
**Действие:**
1. Если холодильник пустой, отправь `/start` и загрузи чек (Шаг 3.3)
2. Нажми "🧊 Холодильник"

**Что проверяю:**
- ✅ Callback `menu_fridge` обрабатывается
- ✅ `show_fridge_summary` показывает количество продуктов
- ✅ Если продуктов нет - показывается сообщение "Холодильник пуст"

### Шаг 4.2: Список продуктов с пагинацией
**Действие:**
1. Если есть продукты, нажми "📋 Показать все"
2. Если продуктов > 10, проверь пагинацию (кнопки "← Назад" / "Далее →")

**Что проверяю:**
- ✅ `list_fridge` вызывается с правильным `page`
- ✅ SQL запрос с `LIMIT` и `OFFSET`
- ✅ Callback `fridge_list:page` обрабатывается
- ✅ Пагинация работает корректно (переход между страницами)

### Шаг 4.3: Детальный просмотр продукта
**Действие:**
1. Нажми на любой продукт из списка

**Что проверяю:**
- ✅ Callback `fridge_item:product_id:page` обрабатывается
- ✅ `show_product_detail` показывает детали продукта
- ✅ SQL запрос к `Product` и `Receipt` таблицам
- ✅ Кнопки "Съел" и "Удалить" отображаются

### Шаг 4.4: Удаление продукта
**Действие:**
1. Нажми "🗑️ Удалить" на любом продукте
2. Подтверди удаление

**Что проверяю:**
- ✅ Callback `delete_product:product_id:page` обрабатывается
- ✅ SQL DELETE запрос выполняется
- ✅ `session.delete(product)` и `session.commit()`
- ✅ Обновление списка продуктов (refresh)
- ✅ Логирование удаления (`Product deleted`)

**Ожидаемый результат:**
- Продукт удалён из БД
- Список обновляется автоматически

### Шаг 4.5: Потребление продукта (Съел)
**Действие:**
1. Нажми "🍽️ Съел" на любом продукте
2. Подтверди потребление

**Что проверяю:**
- ✅ Callback `eat_product:product_id:page` обрабатывается
- ✅ Создание записи в `ConsumptionLog`
- ✅ SQL INSERT в `consumption_logs` таблицу
- ✅ Обновление списка продуктов (refresh)
- ✅ Логирование (`Product consumed`)

**Ожидаемый результат:**
- Продукт помечен как съеденный
- Запись в логе потребления создана

---

## ⏳ ЭТАП 1.2: Консультант (AI рекомендации по продуктам)

### Шаг 1.2.1: Консультации при загрузке чека
**Действие:**
1. Загрузи чек с продуктами (после прохождения onboarding)
2. Дождись обработки

**Что проверяю:**
- ✅ После обработки чека вызывается `ConsultantService.analyze_products()`
- ✅ Получение `UserSettings` пользователя
- ✅ Если `is_initialized=False` - консультации не показываются
- ✅ Если `is_initialized=True` - показываются рекомендации
- ✅ Формат: предупреждения, рекомендации, предложения

**Ожидаемый результат:**
- После итогового сообщения показывается блок с рекомендациями

### Шаг 1.2.2: Консультации в холодильнике
**Действие:**
1. Открой холодильник
2. Выбери любой продукт для детального просмотра

**Что проверяю:**
- ✅ При просмотре деталей вызывается `ConsultantService.analyze_product()`
- ✅ Рекомендации показываются под информацией о продукте
- ✅ Контекст передается как "fridge"

**Ожидаемый результат:**
- В детальном просмотре продукта есть блок с рекомендациями

### Шаг 1.2.3: Консультации в списке покупок
**Действие:**
1. Открой список покупок
2. Добавь новый товар

**Что проверяю:**
- ✅ После добавления вызывается `ConsultantService.analyze_product()`
- ✅ Создается временный Product объект для анализа
- ✅ Рекомендации показываются в ответном сообщении
- ✅ Контекст передается как "shopping_list"

**Ожидаемый результат:**
- После добавления товара показываются рекомендации

### Шаг 1.2.4: Консультации в режиме покупок
**Действие:**
1. Запусти режим покупок
2. Отсканируй этикетку продукта

**Что проверяю:**
- ✅ После распознавания этикетки вызывается `ConsultantService.analyze_product()`
- ✅ Создается временный Product объект из данных этикетки
- ✅ Рекомендации показываются в сообщении о добавлении в корзину
- ✅ Контекст передается как "shopping"

**Ожидаемый результат:**
- После сканирования этикетки показываются рекомендации

### Шаг 1.2.5: Проверка AI консультанта
**Действие:**
1. Загрузи чек с высококалорийным продуктом (если цель - похудеть)
2. Загрузи чек с полезным продуктом

**Что проверяю:**
- ✅ AI генерирует персонализированные рекомендации
- ✅ Учитывается профиль пользователя (пол, рост, вес, цель)
- ✅ Учитываются аллергии
- ✅ Формат ответа: JSON с warnings, recommendations, missing
- ✅ Fallback на простые правила если AI недоступен

**Ожидаемый результат:**
- Умные, персонализированные рекомендации на основе профиля

---

## ✅ ЭТАП 5: Генерация рецептов (Recipes)

### Шаг 5.1: Просмотр рецептов
**Действие:**
1. Вернись в главное меню
2. Нажми "👨‍🍳 Рецепты"

**Что проверяю:**
- ✅ Callback `menu_recipes` обрабатывается
- ✅ Показывается меню категорий рецептов
- ✅ Кнопки категорий отображаются

### Шаг 5.2: Генерация рецептов по категории
**Действие:**
1. Нажми любую категорию (например, "🍳 Завтрак")
2. Дождись генерации (может занять 10-30 секунд)

**Что проверяю:**
- ✅ Callback `generate_recipes:category` обрабатывается
- ✅ Проверка наличия продуктов в холодильнике
- ✅ Если продуктов нет - показывается сообщение
- ✅ `AIService.generate_recipes()` вызывается с правильными параметрами
- ✅ API запрос к OpenRouter/Perplexity
- ✅ Кэширование рецептов (`CachedRecipe` сохранение)
- ✅ Показ рецептов пользователю
- ✅ Нет ошибок API

**Ожидаемый результат:**
- Бот показывает 3-5 рецептов с ингредиентами и инструкциями
- Рецепты соответствуют категории

### Шаг 5.3: Проверка кэширования рецептов
**Действие:**
1. Нажми ту же категорию ещё раз

**Что проверяю:**
- ✅ Проверка кэша (`get_cached_recipes`)
- ✅ Если рецепты в кэше - показываются из кэша (быстро)
- ✅ Если нет - генерируются заново

---

## ✅ ЭТАП 6: Статистика (Stats)

### Шаг 6.1: Просмотр статистики
**Действие:**
1. Вернись в главное меню
2. Нажми "📊 Статистика"

**Что проверяю:**
- ✅ Callback `menu_stats` обрабатывается
- ✅ SQL запросы к `ConsumptionLog`, `Product`, `Receipt`
- ✅ Подсчёт калорий, белков, жиров, углеводов
- ✅ Показ статистики за период
- ✅ Нет ошибок при агрегации данных

**Ожидаемый результат:**
- Показывается статистика потребления (КБЖУ)
- Графики или текстовые данные

---

## ✅ ЭТАП 7: Список покупок (Shopping List)

### Шаг 7.1: Просмотр списка покупок
**Действие:**
1. Вернись в главное меню
2. Нажми "📝 Список покупок"

**Что проверяю:**
- ✅ Callback `menu_shopping_list` обрабатывается
- ✅ SQL запрос к `shopping_list_items`
- ✅ Показ списка покупок (если есть)

### Шаг 7.2: Добавление в список покупок
**Действие:**
1. Нажми "➕ Добавить"
2. Введи название продукта
3. Подтверди добавление

**Что проверяю:**
- ✅ FSM состояние `ShoppingList.adding_item` устанавливается
- ✅ Сохранение в БД (`ShoppingListItem` создаётся)
- ✅ Обновление списка

### Шаг 7.3: Удаление из списка покупок
**Действие:**
1. Нажми "✅" на любом элементе списка

**Что проверяю:**
- ✅ Callback `complete_item:item_id` обрабатывается
- ✅ SQL DELETE или UPDATE (помечание как выполненного)
- ✅ Обновление списка

---

## ✅ ЭТАП 8: Режим покупок (Shopping Mode)

### Шаг 8.1: Запуск режима покупок
**Действие:**
1. Вернись в главное меню
2. Нажми "🛒 Иду в магазин (AR)"

**Что проверяю:**
- ✅ Callback `start_shopping_mode` обрабатывается
- ✅ FSM состояние `ShoppingMode.waiting_for_receipt` устанавливается
- ✅ Показываются инструкции

### Шаг 8.2: Загрузка чека в режиме покупок
**Действие:**
1. Отправь фото чека в режиме покупок

**Что проверяю:**
- ✅ `handle_photo` определяет состояние `ShoppingMode.waiting_for_receipt`
- ✅ Автоматическая обработка чека (без меню выбора)
- ✅ OCR обработка
- ✅ Сопоставление с продуктами (`MatchingService.match_products`)
- ✅ Показ совпадений с ценами
- ✅ Создание `ShoppingSession`

**Ожидаемый результат:**
- Чек обрабатывается автоматически
- Показываются совпадения продуктов с ценами

### Шаг 8.3: Загрузка ценника
**Действие:**
1. Выйди из режима покупок (или отправь `/start`)
2. Отправь фото ценника
3. Нажми "🏷️ Это ценник"

**Что проверяю:**
- ✅ Callback `price_tag_action` обрабатывается
- ✅ `PriceTagOCRService.process_image()` вызывается
- ✅ OCR обработка ценника
- ✅ Поиск цен через `PriceSearchService`
- ✅ Показ результатов поиска

---

## ✅ ЭТАП 9: Настройки пользователя (User Settings)

### Шаг 9.1: Просмотр настроек
**Действие:**
1. Вернись в главное меню
2. Нажми "⚙️ Настройки"
3. Нажми любую кнопку настроек (если есть)

**Что проверяю:**
- ✅ Callback `menu_settings` обрабатывается
- ✅ Если есть подменю - проверка навигации
- ✅ FSM состояния для ввода настроек

### Шаг 9.2: Установка целей (калории, КБЖУ)
**Действие:**
1. Нажми "🎯 Цели питания" (если есть)
2. Введи калории (например, 2000)
3. Введи белки (например, 150)
4. Введи жиры (например, 60)
5. Введи углеводы (например, 250)

**Что проверяю:**
- ✅ FSM состояния `set_calories`, `set_proteins`, `set_fats`, `set_carbs`
- ✅ Валидация введённых значений
- ✅ Сохранение в БД (`UserSettings` создаётся/обновляется)
- ✅ SQL INSERT/UPDATE в `user_settings` таблицу

**Ожидаемый результат:**
- Настройки сохранены
- Можно проверить в статистике

### Шаг 9.3: Установка аллергий
**Действие:**
1. Нажми "🚫 Аллергии" (если есть)
2. Введи список аллергий (например, "молоко, орехи")

**Что проверяю:**
- ✅ FSM состояние `set_allergies`
- ✅ Сохранение в БД
- ✅ Использование при генерации рецептов (фильтрация)

---

## ✅ ЭТАП 10: Коррекция продуктов (Correction) - ПРОТЕСТИРОВАНО

### Шаг 10.1: Выбор поля для редактирования
**Действие:**
1. Перейди в холодильник или нажми "✏️ Коррекция" на продукте из чека
2. Нажми "✏️ Коррекция" на любом продукте

**Что проверяю:**
- ✅ Callback `correct_{product_id}` обрабатывается
- ✅ Показывается карточка продукта с кнопками:
  - "1. Название"
  - "2. 💵 Цена"
  - "3. Количество"
  - "4. 🏷️ Категория"

**Ожидаемый результат:**
- Показывается карточка продукта с текущими данными
- Отображаются 4 кнопки для выбора поля редактирования

### Шаг 10.2: Редактирование названия
**Действие:**
1. Нажми "1. Название"
2. Введи новое название продукта

**Что проверяю:**
- ✅ Callback `edit_name_{product_id}` обрабатывается
- ✅ FSM состояние `CorrectionStates.waiting_for_name` устанавливается
- ✅ Запрос нового названия
- ✅ Обновление названия в БД (`Product.name` обновляется)
- ✅ SQL UPDATE запрос
- ✅ Показ обновлённой карточки продукта

**Ожидаемый результат:**
- Название продукта обновлено
- Отображается обновлённая карточка с кнопкой "✏️ Коррекция"

### Шаг 10.3: Редактирование цены
**Действие:**
1. Нажми "✏️ Коррекция" на продукте
2. Нажми "2. 💵 Цена"
3. Введи новую цену (например, 99.99)

**Что проверяю:**
- ✅ Callback `edit_price_{product_id}` обрабатывается
- ✅ FSM состояние `CorrectionStates.waiting_for_price` устанавливается
- ✅ Валидация введённой цены (число, положительное)
- ✅ Обновление цены в БД (`Product.price` обновляется)
- ✅ SQL UPDATE запрос

**Ожидаемый результат:**
- Цена продукта обновлена
- Отображается обновлённая карточка

### Шаг 10.4: Редактирование количества
**Действие:**
1. Нажми "✏️ Коррекция" на продукте
2. Нажми "3. Количество"
3. Введи новое количество (например, 2.5)

**Что проверяю:**
- ✅ Callback `edit_quantity_{product_id}` обрабатывается
- ✅ FSM состояние `CorrectionStates.waiting_for_quantity` устанавливается
- ✅ Валидация введённого количества (число, положительное)
- ✅ Обновление количества в БД (`Product.quantity` обновляется)
- ✅ SQL UPDATE запрос

**Ожидаемый результат:**
- Количество продукта обновлено
- Отображается обновлённая карточка

### Шаг 10.5: Редактирование категории
**Действие:**
1. Нажми "✏️ Коррекция" на продукте
2. Нажми "4. 🏷️ Категория"
3. Выбери категорию из списка кнопок

**Что проверяю:**
- ✅ Callback `edit_category_{product_id}` обрабатывается
- ✅ Показывается список категорий (13 категорий, по 2 кнопки в ряд)
- ✅ Callback `set_category_{product_id}_{category_index}` обрабатывается
- ✅ Обновление категории в БД (`Product.category` обновляется)
- ✅ SQL UPDATE запрос

**Доступные категории:**
- Молочные продукты
- Мясные продукты
- Овощи
- Фрукты
- Снеки
- Бакалея
- Хлебобулочные изделия
- Напитки
- Сладости
- Замороженные продукты
- Консервы
- Прочее
- Корм для животных

**Ожидаемый результат:**
- Категория продукта обновлена
- Отображается обновлённая карточка с новой категорией

---

## ✅ ЭТАП 11: Обработка ошибок и edge cases

### Шаг 11.1: Обработка пустого фото
**Действие:**
1. Отправь боту фото без текста (например, пейзаж)

**Что проверяю:**
- ✅ OCR не падает с ошибкой
- ✅ Показывается сообщение об ошибке пользователю
- ✅ Логирование ошибки (`ERROR` уровень)
- ✅ Бот продолжает работать

### Шаг 11.2: Обработка некорректного ввода
**Действие:**
1. В режиме ввода настроек отправь текст вместо числа

**Что проверяю:**
- ✅ Валидация ввода
- ✅ Сообщение об ошибке пользователю
- ✅ FSM состояние не сбрасывается некорректно

### Шаг 11.3: Обработка отсутствия продуктов
**Действие:**
1. Удали все продукты из холодильника
2. Попробуй сгенерировать рецепты

**Что проверяю:**
- ✅ Показывается сообщение "Нет продуктов в холодильнике"
- ✅ Нет ошибок при пустом результате SQL запроса

---

## 📊 Чек-лист проверки логов

После каждого этапа я проверю:

### ✅ Успешные операции
- [ ] Все callback'и обрабатываются без ошибок
- [ ] SQL запросы выполняются успешно
- [ ] API запросы (OCR, AI) возвращают данные
- [ ] FSM состояния меняются корректно
- [ ] Изображения загружаются без ошибок

### ⚠️ Ошибки и предупреждения
- [ ] Нет `ERROR` уровня в логах (кроме ожидаемых)
- [ ] Нет `WARNING` о таймаутах
- [ ] Нет ошибок подключения к БД
- [ ] Нет ошибок API (429, 500, etc.)
- [ ] Нет ошибок обработки изображений

### 🚀 Производительность
- [ ] OCR обработка < 60 секунд
- [ ] Генерация рецептов < 30 секунд
- [ ] SQL запросы < 1 секунды
- [ ] Отклик бота на callback < 2 секунды

### 🔒 Безопасность
- [ ] Нет SQL injection уязвимостей
- [ ] Нет утечек токенов в логах
- [ ] Валидация пользовательского ввода

---

## 🎯 Итоговый отчёт

После завершения всех этапов я предоставлю:

1. **Сводка по этапам:**
   - ✅ Работает / ❌ Не работает
   - Время выполнения операций
   - Количество ошибок

2. **Анализ логов:**
   - Список всех ошибок (если есть)
   - Предупреждения
   - Рекомендации по улучшению

3. **Метрики производительности:**
   - Среднее время OCR
   - Среднее время генерации рецептов
   - Время отклика бота

4. **Рекомендации:**
   - Что нужно исправить
   - Что можно оптимизировать
   - Что работает идеально

---

## 🚀 Продолжаем тестирование!

**Текущий статус:** Протестированы базовые функции (инициализация, обработка чека, коррекция).

**Следующий этап:** ЭТАП 4 - Управление холодильником

**Команды для управления ботом:**
```bash
# Перезапуск бота
cd /home/user1/foodflow-bot
pm2 restart foodflow-bot

# Просмотр логов
pm2 logs foodflow-bot --lines 50 --nostream

# Просмотр логов в реальном времени
pm2 logs foodflow-bot
```

**Для мониторинга логов в реальном времени:**
```bash
tail -f foodflow.log
```

Продолжаем! 🎉


